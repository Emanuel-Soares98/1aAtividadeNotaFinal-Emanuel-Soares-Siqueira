<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Calculadora</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #f4f4f4;
    }

    .container {
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      width: 300px;
      text-align: center;
    }

    label {
      display: block;
      text-align: left;
      margin-bottom: 5px;
      color: #333;
    }

    input {
      display: block;
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      box-sizing: border-box;
    }

    button {
      display: block;
      width: 50%;
      margin: 0 auto 15px;
      padding: 10px;
      background-color: blue;
      color: white;
      border: none;
      font-weight: bold;
      border-radius: 5px;
    }

    #result {
      margin-top: 15px;
      font-weight: bold;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Calculadora de IMC</h1>

    <label for="numero1">Peso(kg):</label>
    <input type="number" id="numero1">

    <label for="numero2">Altura(m):</label>
    <input type="number" id="numero2">

    <button type="button" onclick="imc()">Calcular</button>

    <p id="result">IMC:</p>
  </div>

  <script>
    function imc() {
      const num1 = Number(document.getElementById('numero1').value);
      const num2 = Number(document.getElementById('numero2').value); 
      
      if (!num1 || !num2) {
        document.getElementById('result').textContent = 'Por favor, preencha peso e altura corretamente.';
        return;
    }
      const total = num1 / (num2 * num2);
      document.getElementById('result').textContent = `Seu IMC é: ${total.toFixed(2)}`;
    }
  </script>
</body>
</html>
